"""
[DEPRECATED / 已废弃]

Feishu OCR Extraction — Screenshot + OCR fallback.
飞书 OCR 提取 — 截图 + OCR 兜底方案。

This module is no longer used. The CDP PageMain approach (feishu_cdp.py)
handles all document types reliably without OCR.
此模块已不再使用。CDP PageMain 方案（feishu_cdp.py）可靠处理所有文档类型，无需 OCR。

Kept for reference only. / 仅保留供参考。
"""

# All code below is commented out — not loaded, no dependencies required.
# 以下代码已全部注释 — 不会加载，不需要额外依赖。
